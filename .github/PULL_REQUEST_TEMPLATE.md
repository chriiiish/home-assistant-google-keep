## Summary

<!-- What does this PR change, and why? -->

## Type of change

- [ ] Bug fix
- [ ] New feature
- [ ] Breaking change
- [ ] Documentation only

## How was this tested?

<!--
This integration relies on Google's Keep API and OAuth2, which are hard to
exercise in CI. Describe what you actually verified, e.g.:
- Ran against a real Home Assistant instance + real Google account
- Unit-tested the pure logic (item parsing, uid hashing, move/delete math)
- Syntax/import checked only
-->

## Checklist

- [ ] I ran `ruff check` and `ruff format --check` locally (CI also runs this)
- [ ] I updated the README if behavior or setup steps changed
- [ ] I bumped `manifest.json` `version` if this is a user-facing change
